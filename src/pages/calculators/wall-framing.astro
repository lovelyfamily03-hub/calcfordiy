---
import Layout from '../../layouts/Layout.astro';
---

<Layout
  title="Wall Framing Calculator — Studs & Plates Estimate | CalcForDIY"
  description="Calculate exactly how many studs and linear feet of plates you need for a partition wall. Automatically accounts for corners, doors, windows, and waste factor."
>

  <script type="application/ld+json" is:inline>
  {
    "@context": "https://schema.org",
    "@type": "WebApplication",
    "name": "Wall Framing Calculator",
    "description": "Free framing calculator that estimates studs and plates needed for walls based on length, spacing, corners, and openings.",
    "applicationCategory": "CalculatorApplication",
    "operatingSystem": "Web Browser",
    "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
  }
  </script>

  <script type="application/ld+json" is:inline>
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [
      {
        "@type": "Question",
        "name": "How many studs do I need for a 10 foot wall?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "For a standard 10-foot wall with 16-inch on-center spacing and no doors or windows, you need 9 field studs. When you add a 10% waste factor, you should purchase 10 studs. If the wall has corners, you must add 3 studs per corner."
        }
      },
      {
        "@type": "Question",
        "name": "How many studs per corner do I need?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Standard framing practice requires 3 studs per corner (often built as a California corner or U-channel). This structural arrangement provides the necessary overlapping backing for drywall installation on interior corners."
        }
      },
      {
        "@type": "Question",
        "name": "How many extra studs do I add for a door or window?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "You should add 4 studs for every door or window opening. This accounts for the 2 full-length King studs and the 2 Jack (trimmer) studs that support the header. The standard 16-inch field studs that fall inside the opening are cut and reused as the short cripple studs."
        }
      },
      {
        "@type": "Question",
        "name": "Should I use 16-inch or 24-inch stud spacing?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "16-inch on-center is the standard for most residential walls, providing maximum rigidity for drywall. 24-inch spacing is sometimes used for non-load-bearing interior partition walls to save lumber and increase insulation space, but requires thicker 5/8-inch drywall to prevent bowing between studs."
        }
      },
      {
        "@type": "Question",
        "name": "Do I need a double top plate?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Yes, standard framing practice and the IRC mandate a double top plate. The second plate allows you to overlap joints and firmly tie intersecting walls together, significantly increasing the structural integrity of the home."
        }
      }
    ]
  }
  </script>

  <script type="application/ld+json" is:inline>
  {
    "@context": "https://schema.org",
    "@type": "BreadcrumbList",
    "itemListElement": [
      { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://calcfordiy.com" },
      { "@type": "ListItem", "position": 2, "name": "Calculators", "item": "https://calcfordiy.com/calculators" },
      { "@type": "ListItem", "position": 3, "name": "Wall Framing Calculator", "item": "https://calcfordiy.com/calculators/wall-framing" }
    ]
  }
  </script>

  <div class="flex items-center gap-2 text-sm text-gray-500 mb-4 print:hidden">
    <a href="/" class="hover:text-blue-600">Home</a>
    <span>/</span>
    <a href="/calculators" class="hover:text-blue-600">Calculators</a>
    <span>/</span>
    <span>Wall Framing Calculator</span>
  </div>

  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900 mb-3">Wall Framing Calculator</h1>
    <p class="text-gray-600 text-lg">Calculate exactly how many studs and linear feet of plates you need for a partition wall, including accurate material counts for corners, doors, and windows.</p>
  </div>

  <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 mb-6">
    <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-5">
      <h2 class="text-base font-semibold text-gray-800 flex items-center gap-2">
        <span class="bg-blue-100 text-blue-700 rounded-full w-6 h-6 flex items-center justify-center text-xs font-bold">1</span>
        Global Settings
      </h2>
      <div class="flex items-center gap-3">
        <label class="text-sm font-medium text-gray-700">Stud Spacing:</label>
        <select id="spacing" class="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 bg-gray-50">
          <option value="16" selected>16" on-center</option>
          <option value="24">24" on-center</option>
        </select>
      </div>
    </div>

    <hr class="my-5 border-gray-200" />

    <h2 class="text-base font-semibold text-gray-800 mb-4 flex items-center gap-2">
      <span class="bg-blue-100 text-blue-700 rounded-full w-6 h-6 flex items-center justify-center text-xs font-bold">2</span>
      Wall Dimensions
    </h2>
    
    <div class="bg-blue-50 border border-blue-200 text-blue-800 text-sm rounded-lg p-3 mb-4">
      <strong>Pro Tip:</strong> When framing multiple rooms, do not double-count shared walls! Enter long continuous walls as a single entry, then enter the interior partition walls that butt into them separately. Count every place a wall meets another wall as an Intersection below.
    </div>

    <div id="wallsContainer" class="space-y-4">
      <div class="wall-entry p-4 bg-gray-50 rounded-lg border border-gray-200 relative">
        <h3 class="font-semibold text-gray-800 mb-3 wall-title">Wall 1</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Wall Length</label>
            <div class="flex items-center gap-2">
              <input type="number" class="len_ft w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="10" min="0" max="200" />
              <span class="text-xs text-gray-500 shrink-0">ft</span>
              <input type="number" class="len_in w-16 border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="0" min="0" max="11" />
              <span class="text-xs text-gray-500 shrink-0">in</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <button type="button" id="addWallBtn" class="mt-4 w-full border-2 border-dashed border-gray-300 hover:border-blue-400 text-gray-600 hover:text-blue-600 font-medium py-3 rounded-lg transition-colors flex items-center justify-center gap-2 text-sm">
      + Add Another Wall
    </button>

    <hr class="my-5 border-gray-200" />

    <h2 class="text-base font-semibold text-gray-800 mb-4 flex items-center gap-2">
      <span class="bg-blue-100 text-blue-700 rounded-full w-6 h-6 flex items-center justify-center text-xs font-bold">3</span>
      Project Openings & Intersections
    </h2>

    <div class="p-4 bg-gray-50 rounded-lg mb-5">
      <p class="text-xs text-gray-500 mb-3">Total count for your entire project. Extra framing is automatically calculated based on IRC standards (+3 studs per intersection, +4 per opening).</p>
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div>
          <label class="block text-xs text-gray-500 mb-1">Corners & T-Intersections</label>
          <input type="number" id="global_corners" placeholder="0" min="0" max="50" class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" />
        </div>
        <div>
          <label class="block text-xs text-gray-500 mb-1">Total Doors</label>
          <input type="number" id="global_doors" placeholder="0" min="0" max="20" class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" />
        </div>
        <div>
          <label class="block text-xs text-gray-500 mb-1">Total Windows</label>
          <input type="number" id="global_windows" placeholder="0" min="0" max="20" class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" />
        </div>
      </div>
    </div>

    <button type="button" id="addWallBtn" class="mt-4 w-full border-2 border-dashed border-gray-300 hover:border-blue-400 text-gray-600 hover:text-blue-600 font-medium py-3 rounded-lg transition-colors flex items-center justify-center gap-2 text-sm">
      + Add Another Wall
    </button>

    <hr class="my-5 border-gray-200" />

    <button onclick="calculate()" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors text-base">
      Calculate Materials Needed →
    </button>
  </div>

  <div id="errorMsg" class="hidden bg-red-50 border border-red-200 rounded-lg p-4 mb-6 text-red-700 text-sm">
    Please enter a valid wall length to calculate.
  </div>

  <div id="results" class="hidden bg-gradient-to-br from-blue-50 to-blue-100 border border-blue-200 rounded-xl p-6 mb-8">
    <h2 class="text-lg font-semibold text-blue-900 mb-4">Your Framing Estimate</h2>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-4">
      <div class="bg-white rounded-lg p-4 text-center shadow-sm">
        <div class="text-2xl font-bold text-gray-900" id="r_field">—</div>
        <div class="text-xs text-gray-500 mt-1">Field Studs</div>
      </div>
      <div class="bg-white rounded-lg p-4 text-center shadow-sm">
        <div class="text-2xl font-bold text-gray-900" id="r_extras">—</div>
        <div class="text-xs text-gray-500 mt-1">Corner/Opening Studs</div>
      </div>
      <div class="bg-white rounded-lg p-4 text-center shadow-sm border-2 border-blue-400">
        <div class="text-2xl font-bold text-blue-700" id="r_total">—</div>
        <div class="text-xs text-gray-700 font-medium mt-1">Total Studs (+10% Waste)</div>
      </div>
      <div class="bg-white rounded-lg p-4 text-center shadow-sm border-2 border-green-400">
        <div class="text-2xl font-bold text-green-700"><span id="r_plates">—</span> ft</div>
        <div class="text-xs text-gray-700 font-medium mt-1">Total Plate Lumber</div>
      </div>
    </div>
    <p class="text-xs text-blue-800 mt-4 bg-blue-200 bg-opacity-50 rounded-lg p-3">
      <strong>Note:</strong> Plate lumber assumes 1 bottom plate and 2 top plates (standard IRC requirement). Plate calculation includes a 5% waste factor for trimming. 
    </p>
    <button id="printBtn" onclick="window.print()" class="hidden mt-5 w-full bg-gray-800 hover:bg-gray-900 text-white font-semibold py-3 px-6 rounded-lg transition-colors print:hidden text-base flex justify-center items-center gap-2">
      🖨️ Print or Save as PDF
    </button>
  </div>

  <div class="bg-white border border-gray-200 rounded-xl p-6 mb-6">
    <h2 class="text-xl font-semibold text-gray-900 mb-4">How to Use This Calculator</h2>
    <p class="text-gray-700 mb-4">Using this wall framing calculator ensures you order the exact right amount of lumber without overpaying or running short. Here is how to use it:</p>
    <ol class="space-y-4 text-gray-700">
      <li class="flex gap-3">
        <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold shrink-0 mt-0.5">1</span>
        <span><strong>Set the global stud spacing.</strong> Choose between standard 16-inch on-center spacing (most common) or 24-inch spacing. This setting applies to all walls in your project.</span>
      </li>
      <li class="flex gap-3">
        <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold shrink-0 mt-0.5">2</span>
        <span><strong>Enter the wall length.</strong> Measure the total linear length of your first wall. You can enter feet and inches separately for precision.</span>
      </li>
      <li class="flex gap-3">
        <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold shrink-0 mt-0.5">3</span>
        <span><strong>Add multiple walls.</strong> If you are framing an entire room or basement, click the <strong>+ Add Another Wall</strong> button. You can add as many individual walls as you need.</span>
      </li>
      <li class="flex gap-3">
        <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold shrink-0 mt-0.5">4</span>
        <span><strong>Enter total corners and openings.</strong> Instead of entering doors and windows for every single wall, simply tally up your project's total corners, T-intersections, doors, and windows at the bottom. The calculator will accurately sum the total lumber for your entire project!</span>
      </li>
    </ol>
  </div>

  <div class="bg-white border border-gray-200 rounded-xl p-6 mb-6">
    <h2 class="text-xl font-semibold text-gray-900 mb-4">The Formula</h2>
    <p class="text-gray-700 mb-4">This framing calculator utilizes standard IRC residential building codes and professional estimating rules to output your final lumber list. Here is the math:</p>
    <div class="bg-gray-50 rounded-lg p-4 mb-5 font-mono text-sm text-gray-800 space-y-2">
      <p>Field Studs = (Wall Length ÷ Stud Spacing) + 1</p>
      <p>Corner Studs = Corners × 3</p>
      <p>Opening Studs = (Doors + Windows) × 4</p>
      <p>Total Studs = (Field + Corner + Opening) × 1.10 (Waste)</p>
      <p>Total Plates = Wall Length × 3</p>
    </div>
    <p class="text-gray-700">First, the calculator determines the base "field" studs by dividing the wall length by your selected spacing and adding one for the end stud. It then adds 3 extra studs for every corner to create standard U-channels (California corners). For openings, it adds 4 extra studs per door or window (2 kings and 2 jacks). The field studs that fall inside the openings are assumed to be cut and repurposed as cripple studs, so no additional lumber is required for those.</p>
    <p class="text-gray-700 mt-3">Finally, a professional 10% waste factor is applied to the total vertical stud count to account for off-cuts and warped boards. For the horizontal plates, the length of the wall is multiplied by 3 (assuming standard 1 bottom plate and 2 top plates) plus a 5% waste factor.</p>
  </div>

  <div class="bg-white border border-gray-200 rounded-xl p-6 mb-6">
    <h2 class="text-xl font-semibold text-gray-900 mb-5">Frequently Asked Questions</h2>
    <div class="space-y-5">
      <div>
        <h3 class="font-semibold text-gray-900 mb-1">How do I measure shared partition walls?</h3>
        <p class="text-gray-600 text-sm">When framing multiple adjoining rooms, do not double-count a shared wall by measuring Room A's perimeter and then Room B's perimeter. Frame the long, continuous wall first and enter it as a single wall. Then, build the interior partition walls so they butt up against it. In the calculator, count every place a partition wall meets another wall as a "T-Intersection" (which uses 3 extra studs for backing, identical to a corner).</p>
      </div>
      <div>
        <h3 class="font-semibold text-gray-900 mb-1">How many studs do I need for a 10 foot wall?</h3>
        <p class="text-gray-600 text-sm">For a standard 10-foot wall with 16-inch on-center spacing and no doors or windows, you need 9 field studs. When you add a 10% waste factor, you should purchase 10 studs. If the wall has corners, you must add 3 studs per corner.</p>
      </div>
      <div>
        <h3 class="font-semibold text-gray-900 mb-1">How many studs per corner do I need?</h3>
        <p class="text-gray-600 text-sm">Standard framing practice requires 3 studs per corner (often built as a California corner or U-channel). This structural arrangement provides the necessary overlapping backing for drywall installation on interior corners.</p>
      </div>
      <div>
        <h3 class="font-semibold text-gray-900 mb-1">How many extra studs do I add for a door or window?</h3>
        <p class="text-gray-600 text-sm">You should add 4 studs for every door or window opening. This accounts for the 2 full-length King studs and the 2 Jack (trimmer) studs that support the header. The standard 16-inch field studs that fall inside the opening are cut and reused as the short cripple studs.</p>
      </div>
      <div>
        <h3 class="font-semibold text-gray-900 mb-1">Should I use 16-inch or 24-inch stud spacing?</h3>
        <p class="text-gray-600 text-sm">16-inch on-center is the standard for most residential walls, providing maximum rigidity for drywall. 24-inch spacing is sometimes used for non-load-bearing interior partition walls to save lumber and increase insulation space, but requires thicker 5/8-inch drywall to prevent bowing between studs.</p>
      </div>
      <div>
        <h3 class="font-semibold text-gray-900 mb-1">Do I need a double top plate?</h3>
        <p class="text-gray-600 text-sm">Yes, standard framing practice and the IRC mandate a double top plate. The second plate allows you to overlap joints and firmly tie intersecting walls together, significantly increasing the structural integrity of the home.</p>
      </div>
    </div>
  </div>

  <div class="bg-gray-50 border border-gray-200 rounded-xl p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">Related Calculators</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
      <a href="/calculators/drywall" class="block bg-white p-4 rounded-lg border border-gray-200 hover:border-blue-400 hover:shadow-sm transition-all">
        <div class="font-medium text-gray-900 text-sm">Drywall Calculator</div>
        <div class="text-xs text-gray-500 mt-1">Calculate how many sheets of drywall you need to cover your newly framed walls.</div>
      </a>
      <a href="/calculators/paint-room" class="block bg-white p-4 rounded-lg border border-gray-200 hover:border-blue-400 hover:shadow-sm transition-all">
        <div class="font-medium text-gray-900 text-sm">Paint Calculator for a Room</div>
        <div class="text-xs text-gray-500 mt-1">Estimate exactly how many gallons of paint you need for your finished walls.</div>
      </a>
      <a href="/calculators/subfloor" class="block bg-white p-4 rounded-lg border border-gray-200 hover:border-blue-400 hover:shadow-sm transition-all">
        <div class="font-medium text-gray-900 text-sm">Subfloor Calculator</div>
        <div class="text-xs text-gray-500 mt-1">Calculate how many sheets of OSB or plywood you need for the floor beneath your framing.</div>
      </a>
    </div>
  </div>

</Layout>

<script is:inline>
  function addWall() {
    const container = document.getElementById('wallsContainer');
    const wallCount = container.querySelectorAll('.wall-entry').length + 1;
    
    const wallHTML = `
      <div class="wall-entry p-4 bg-gray-50 rounded-lg border border-gray-200 relative mt-4">
        <div class="flex justify-between items-center mb-3">
          <h3 class="font-semibold text-gray-800 wall-title">Wall ${wallCount}</h3>
          <button type="button" class="text-red-500 hover:text-red-700 text-sm font-medium remove-wall-btn">Remove</button>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">Wall Length</label>
            <div class="flex items-center gap-2">
              <input type="number" class="len_ft w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="10" min="0" max="200" />
              <span class="text-xs text-gray-500 shrink-0">ft</span>
              <input type="number" class="len_in w-16 border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="0" min="0" max="11" />
              <span class="text-xs text-gray-500 shrink-0">in</span>
            </div>
          </div>
        </div>
      </div>
    `;
    
    container.insertAdjacentHTML('beforeend', wallHTML);
    attachListeners();
  }

  function removeWall(btn) {
    btn.closest('.wall-entry').remove();
    const walls = document.querySelectorAll('.wall-entry');
    walls.forEach((wall, idx) => {
      wall.querySelector('.wall-title').textContent = `Wall ${idx + 1}`;
    });
    calculate();
  }

  function attachListeners() {
    document.querySelectorAll('.wall-entry input').forEach(el => {
      el.removeEventListener('input', calculate);
      el.addEventListener('input', calculate);
    });
    document.querySelectorAll('.remove-wall-btn').forEach(btn => {
      btn.removeEventListener('click', function() { removeWall(this); });
      btn.addEventListener('click', function() { removeWall(this); });
    });
    document.getElementById('global_corners')?.addEventListener('input', calculate);
    document.getElementById('global_doors')?.addEventListener('input', calculate);
    document.getElementById('global_windows')?.addEventListener('input', calculate);
  }

  function calculate() {
    let totalFieldStuds = 0;
    let totalLenFtForPlates = 0;
    let hasValidLength = false;
    
    const spacing = parseFloat(document.getElementById('spacing').value) || 16;
    
    document.querySelectorAll('.wall-entry').forEach(wall => {
      const lenFt = parseFloat(wall.querySelector('.len_ft').value) || 0;
      const lenIn = parseFloat(wall.querySelector('.len_in').value) || 0;
      
      if (lenFt > 0 || lenIn > 0) {
        hasValidLength = true;
      }
      
      const totalLenFt = lenFt + (lenIn / 12);
      const totalLenIn = (lenFt * 12) + lenIn;
      totalLenFtForPlates += totalLenFt;
      
      if (totalLenIn > 0) {
        totalFieldStuds += Math.ceil(totalLenIn / spacing) + 1;
      }
    });
    
    if (!hasValidLength) {
      document.getElementById('errorMsg').classList.remove('hidden');
      document.getElementById('results').classList.add('hidden');
      return;
    }
    document.getElementById('errorMsg').classList.add('hidden');

    const corners = parseInt(document.getElementById('global_corners').value) || 0;
    const doors = parseInt(document.getElementById('global_doors').value) || 0;
    const windows = parseInt(document.getElementById('global_windows').value) || 0;
    const totalExtras = (corners * 3) + (doors * 4) + (windows * 4);

    const totalRawStuds = totalFieldStuds + totalExtras;
    const totalWithWaste = Math.ceil(totalRawStuds * 1.10); // 10% waste
    
    const platesLinearFt = totalLenFtForPlates * 3; // 1 bottom + 2 top
    const platesWithWaste = platesLinearFt * 1.05; // 5% waste

    document.getElementById('r_field').innerText = totalFieldStuds;
    document.getElementById('r_extras').innerText = totalExtras;
    document.getElementById('r_total').innerText = totalWithWaste;
    document.getElementById('r_plates').innerText = Math.ceil(platesWithWaste);

    document.getElementById('results').classList.remove('hidden');
    document.getElementById('printBtn').classList.remove('hidden');
  }

  document.addEventListener('DOMContentLoaded', function () {
    attachListeners();
    document.getElementById('spacing').addEventListener('change', calculate);
    document.getElementById('addWallBtn').addEventListener('click', addWall);
  });
</script>
